THROTTLE_BUCKETS = {
    'default': {
        'VARY': 'throttle.strategies.RemoteIP',
        'NUM_BUCKETS': 10, # Number of buckets worth of history to keep. Must be at least 2
        'BUCKET_CAPACITY': 5,
        'BUCKET_TIME': 60*15 # Number of seconds to use each bucket.
    }
}

THROTTLE_BACKEND = 'throttle.backends.cache.CacheBackend'
THROTTLE_BACKEND = 'throttle.backends.redis.RedisBackend'
THROTTLE_BACKEND = 'throttle.backends.model.ModelBackend'
THROTTLE_BACKEND = 'throttle.backends.local.LocMemBackend' # Bad for production

